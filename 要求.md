# 期末作业要求

**《嵌入式软件开发》课程期末综合大作业**

**一、作业说明**

期末综合大作业基于本学期所学OpenHarmony系统开发、BearPi系列（nano和micro）开发板硬件操作、E53扩展模块应用、华为IoT云平台对接及HAP应用部署核心知识，要求独立完成一套完整的智慧场景嵌入式系统设计与实现。作业严格遵循实验室硬件条件限制（单块开发板同一时间仅可接入一个E53模块），重点考查系统设计能力、工程实操能力、全栈部署能力与问题解决能力，全面检验课程学习成果。

**二、考核目标**

\1. **知识目标**：巩固OpenHarmony系统架构、外设驱动开发、网络通信、云平台对接、HAP应用部署等核心知识点，构建完整的鸿蒙嵌入式开发知识体系。

\2. **能力目标**：掌握从需求分析、方案设计、代码开发、编译烧录、调试验证到应用部署、成果展示的全流程工程开发能力；具备独立解决硬件调试、网络通信、应用部署中常见问题的能力。

\3. **素质目标**：培养严谨的工程思维、规范的编程习惯与自主学习能力；树立科技报国信念，增强国产操作系统与物联网技术的应用信心。

**三、作业核心要求**

**（一）基本要求**

\1. **独立完成**：所有学生必须独立完成大作业的设计、开发、调试与文档撰写，严禁抄袭、代做、代码复制，一经发现按作弊处理，本课程成绩记为0分。

\2. **四选一选题**：从以下4个智慧场景中任选其一完成，每个场景均需实现**硬件采集控制****+****华为云平台对接****+HAP****应用安装与远程控制**三大核心功能，可基于现有HAP模板自行添加特色功能、自定义界面图标或优化交互体验。

\3. **硬件分工规范**：统一采用**BearPi-HM Micro****作为传感器采集与执行控制端****BearPi-HM Micro****显示器上安装****HAP****应用运行端**，开发板通过华为云平台实现数据交互与指令传输、通过hap应用控制传感器行为，严格遵守单E53模块使用规则。

 

 

**（二）可选场景及功能要求**

**1.** **智慧农业场景（****E53_IA1** **智慧农业模块）**

**开发板端功能**：周期性采集环境温度、湿度、光照强度数据；通过串口实时打印数据与运行日志；设置光照阈值，光照不足时自动开启补光灯；设置温湿度阈值，数值超标时触发预警；通过Wi-Fi+MQTT将数据与设备状态上传至华为云平台，接收云端下发的控制指令。

**HAP****应用功能**：在BearPi-HM Micro上安装HAP应用，可自行添加特色功能、自定义界面图标或优化交互体验；实时显示温度、湿度、光照强度数据；显示补光灯与预警设备的运行状态；提供补光灯远程开关、阈值手动调整功能；支持查看历史数据曲线。

**2.** **智慧安防场景（****E53_IS1** **人体红外模块）**

**开发板端功能**：实时检测监测区域内人体活动状态；串口打印检测状态与报警记录；检测到非法入侵时自动触发蜂鸣器报警；将设备状态、报警信息实时上传至华为云平台，支持云端远程控制报警开关。

**HAP****应用功能**：在BearPi-HM Micro上安装HAP应用，可自行添加特色功能、自定义界面图标或优化交互体验；实时显示安防区域监测状态；入侵发生时弹窗报警并播放提示音；提供报警设备远程开关、报警记录查询功能；支持一键布防/撤防操作。

**3.** **智慧井盖场景（****E53_SC2** **智慧井盖模块）**

**开发板端功能**：实时检测井盖开合、移位异常状态；串口打印设备运行状态与异常记录；检测到井盖异常时立即启动声光报警；将状态数据、异常告警上传至华为云平台，支持云端远程管控。

**HAP****应用功能**：在BearPi-HM Micro上安装HAP应用，可自行添加特色功能、自定义界面图标或优化交互体验；实时显示井盖在线状态与位置信息；异常发生时高亮提示并推送告警信息；提供报警设备远程复位、异常记录导出功能；支持设备批量管理（扩展功能）。

**4.** **智慧照明场景（****E53_SC1** **智慧台灯模块）**

**开发板端功能**：实现台灯本地按键开关控制；支持PWM亮度调节；串口打印灯光状态与操作记录；将灯光状态、亮度信息上传至华为云平台，接收云端远程控制指令。

**HAP****应用功能**：在BearPi-HM Micro上安装HAP应用，可自行添加特色功能、自定义界面图标或优化交互体验；实时显示台灯开关状态与当前亮度；提供远程开关、滑动条亮度调节功能；支持定时开关灯、场景模式切换（如阅读模式、休息模式）。

**（三）开发与实现要求**

\1. **开发环境**：使用小熊派虚拟机、烧录软件，电脑安装与调试，支持基于现有HAP模板进行功能扩展与界面定制。

\2. **代码规范**：Micro端代码需添加清晰注释，采用模块化编程思想，变量与函数命名规范；工程结构清晰，区分驱动层、业务层、通信层代码。

\3. **云平台要求**：所有设备必须接入华为IoT平台，完成产品创建、模型定义、设备注册；实现属性上报、命令下发、消息通知等核心功能。

\4. **效果呈现**：系统需稳定运行，数据采集准确，控制响应及时；HAP应用安装成功、运行流畅，无明显卡顿与闪退问题；自定义功能或界面优化需在实验报告中详细说明。

**四、提交内容要求**

所有学生需在汇报前提交以下完整材料，打包为压缩包（命名格式：学号-姓名-大作业题目.zip）：

\1. 项目文件：

（1）所选传感器的驱动代码文件、业务文件、模块文件和对应头文件；

（2）HAP配置文件

（3）对接华为云平台的业务代码（包含wifi接入、密钥认证等内容

（4）HAP安装包。

\2. **实验报告**：采用Word格式，内容需包含：需求分析与系统总体设计、硬件选型与接口说明、核心实现步骤（分模块详细描述）、关键业务代码（附注释说明）、HAP应用安装与定制说明、系统测试与结果分析、遇到的问题与解决方案、总结与展望。

\3. **汇报****PPT**：页数控制在5-10页，内容简洁明了、重点突出，包含系统架构、功能演示、核心代码、HAP应用定制效果、成果展示等模块。

**五、评分标准（总分****100****分）**

| 评分维度                     | 分值          | 评分细则                                                     |
| ---------------------------- | ------------- | ------------------------------------------------------------ |
| **一、系统功能实现（核心）** | **40****分**  | （1）所有基础功能完整实现，数据采集准确，控制响应及时良好；（2）成功安装HAP应用并实现所有要求功能，自定义功能或界面优化效果显著，运行流畅良好；（3）华为云平台对接：设备正常接入且激活为在线状态；（4）支持传感器数据上报和命令下发。 |
| **二、文件提交完整性**       | **15****分**  | 根据提交的作业资料文件完整度给分                             |
| **三、实验报告质量**         | **20****分**  | 1. 内容完整性2. 对系统设计、实现难点、问题解决有深入分析，有自己的思考良好3. 格式规范性 |
| **四、汇报与现场演示**       | **15****分**  | 1. PPT制作2. 汇报表达3. 现场演示与答辩                       |
| **五、创新加分项**           | **10****分**  | （1）基于现有HAP进行功能扩展（2）实现多设备联动控制；（3）加入AI辅助决策（如根据历史数据自动调整阈值） |
| **合计**                     | **100****分** | —                                                            |

 

**六、扣分规则**

\1. **抄袭作弊**：发现抄袭、代做、代码复制等行为，本大作业成绩记为0分，并按学校相关规定处理。

\2. **逾期提交**：每逾期1天扣5分，逾期3天及以上成绩记为0分。

\3. **未参加汇报**：未按要求参加现场汇报与演示，本大作业成绩记为0分。

\4. **材料缺失**：提交材料不完整，每缺一项扣10分；HAP应用安装包缺失扣15分。

**七、时间安排**

\1. **第****15****周（本周）**：完成选题、硬件领取与环境搭建；进行系统设计与代码开发；完成硬件调试、云平台对接与HAP应用安装定制；撰写实验报告初稿，制作汇报PPT。

\2. **第****16****周（下周）**：周一：完成系统联调与功能验证；完善实验报告与PPT；周三：按学号顺序进行现场汇报与演示，每人汇报时长5-8分钟，答辩2-3分钟。周五18:00前：提交所有电子版材料至课堂派。

**八、注意事项**

\1. 实验过程中严格遵守实验室规章制度，规范操作开发板与电子设备，防止硬件损坏；实验结束后及时归还硬件器材。

\2. 所有提交材料必须为原创，引用他人成果需注明出处；若发现抄袭行为，取消本次大作业成绩。

\3. 逾期未提交材料或未参加现场汇报者，本大作业成绩记为0分。

\4. 实验过程中遇到问题可通过课程群咨询老师或同学，鼓励相互交流学习，但禁止直接复制代码与文档。

\5. 课程提供统一的基础HAP应用模板，学生可在此基础上进行功能扩展与界面定制，也可自行开发完整HAP应用（额外加分）。

\6. 鼓励使用AI工具完成本次作业。

# 我的选择方案

我选择方案是4

 **智慧照明场景（****E53_SC1** **智慧台灯模块）**

**开发板端功能**：实现台灯本地按键开关控制；支持PWM亮度调节；串口打印灯光状态与操作记录；将灯光状态、亮度信息上传至华为云平台，接收云端远程控制指令。

**HAP****应用功能**：在BearPi-HM Micro上安装HAP应用，可自行添加特色功能、自定义界面图标或优化交互体验；实时显示台灯开关状态与当前亮度；提供远程开关、滑动条亮度调节功能；支持定时开关灯、场景模式切换（如阅读模式、休息模式）。

 

# 我的实验环境

开发版 + Ubuntu18.04 VM虚拟机环境 + 磁盘映射到Windows盘（Y盘）



# 方案四的项目原型

## E53_SC1驱动开发

1. 确定目录结构。在device\st\driver路径下新建E53_SC1文件夹，并创建驱动文件E53_SC1_hdf.c和编译构建文件BUILD.gn 。

   ```
   .
   └─ device        
       └─ st
           └─ drivers
               └─ e53_driver
                   └─ E53_SC1
                       │─ E53_SC1_hdf.c
                       │─ inc
                       └─ src
   ```

2. E53_SC1驱动实现驱动实现包含驱动业务代码和驱动入口注册，在E53_SC1_hdf.c文件中添加以下代码：

   ```C++
   #include <stdint.h>
   #include <string.h>
   
   #include "E53_SC1.h"
   #include "E53_Common.h"
   
   #include "hdf_device_desc.h" 
   #include "hdf_log.h"         
   #include "device_resource_if.h"
   #include "osal_io.h"
   #include "osal_mem.h"
   #include "gpio_if.h"
   #include "osal_time.h"
   
   
   static uint8_t LightStatus;
   static float lux_data;
   
   typedef enum {
       E53_SC1_Start = 0,
       E53_SC1_Stop,
       E53_SC1_Read,
       E53_SC1_SetLight,
   }E53_SC1Ctrl;
   
   int32_t E53_SC1_DriverDispatch(struct HdfDeviceIoClient *client, int cmdCode, struct HdfSBuf *data, struct HdfSBuf *reply)
   {
       int ret = -1;
       char *replay_buf;
   
       HDF_LOGE("E53 driver dispatch");
       if (client == NULL || client->device == NULL) {
           HDF_LOGE("E53 driver device is NULL");
           return HDF_ERR_INVALID_OBJECT;
       }
       switch (cmdCode) {
           case E53_SC1_Start:
               ret = E53_SC1Init();
               if (ret != 0) {
                   HDF_LOGE("E53 SC1 Init err");
                   return HDF_FAILURE;
               }
               ret = HdfSbufWriteString(reply, "E53 SC1 Init successful");
               if (ret == 0) {
                   HDF_LOGE("reply failed");
                   return HDF_FAILURE;
               }
               break;
           case E53_SC1_Stop:
               ret = E53_SC1DeInit();
               if (ret != 0) {
                   HDF_LOGE("E53 SC1 DeInit err");
                   return HDF_FAILURE;
               }
               ret = HdfSbufWriteString(reply, "E53 SC1 DeInit successful");
               if (ret == 0) {
                   HDF_LOGE("reply failed");
                   return HDF_FAILURE;
               }
               break;
           /* 接收到用户态发来的LED_WRITE_READ命令 */
           case E53_SC1_Read:
               ret = E53_SC1ReadData(&lux_data);
               if (ret != 0) {
                   HDF_LOGE("E53 SC1 Read Data err");
                   return HDF_FAILURE;
               }
               replay_buf = OsalMemAlloc(100);
               (void)memset_s(replay_buf, 100, 0, 100);
               sprintf(replay_buf, "{\"Lux\":%.2f,\"LED\":\"%s\"}", lux_data, LightStatus ? "ON" : "OFF");
               /* 把传感器数据写入reply, 可被带至用户程序 */
               if (!HdfSbufWriteString(reply, replay_buf)) {
                   HDF_LOGE("replay is fail");
                   return HDF_FAILURE;
               }
               OsalMemFree(replay_buf);
               break;
           case E53_SC1_SetLight:
               const char* readdata = HdfSbufReadString(data);
               if (strcmp(readdata, "ON") == 0) {
                   E53_SC1LightStatusSet(ON);
                   LightStatus = 1;
               } else if (strcmp(readdata, "OFF") == 0) {
                   E53_SC1LightStatusSet(OFF);
                   LightStatus = 0;
               } else {
                   HDF_LOGE("Command wrong!");
                   return HDF_FAILURE;
               }
               replay_buf = OsalMemAlloc(100);
               (void)memset_s(replay_buf, 100, 0, 100);
               sprintf(replay_buf, "{\"Lux\":%.2f,\"LED\":\"%s\"}", lux_data, LightStatus ? "ON" : "OFF");
               /* 把传感器数据写入reply, 可被带至用户程序 */
               if (!HdfSbufWriteString(reply, replay_buf)) {
                   HDF_LOGE("replay is fail");
                   return HDF_FAILURE;
               }
               OsalMemFree(replay_buf);
               break;
   
           default:
               return HDF_FAILURE;
       }
       return HDF_SUCCESS;
   }
   
   
   //驱动对外提供的服务能力，将相关的服务接口绑定到HDF框架
   static int32_t Hdf_E53_SC1_DriverBind(struct HdfDeviceObject *deviceObject)
   {
       if (deviceObject == NULL) {
           HDF_LOGE("e53 driver bind failed!");
           return HDF_ERR_INVALID_OBJECT;
       }
       static struct IDeviceIoService e53Driver = {
           .Dispatch = E53_SC1_DriverDispatch,
       };
       deviceObject->service = (struct IDeviceIoService *)(&e53Driver);
       HDF_LOGD("E53 driver bind success");
       return HDF_SUCCESS;
   }
   
   // static struct E53_SC1Hooks hooks = {
   //     .E53_SC1Init = sc1Init,
   //     .E53_SC1DeInit = sc1DeInit,
   //     .E53_SC1LightGpioSet = sc1GpioSet,
   //     .E53_SC1_IICWriteFunc = sc1E53_IICWrite,
   //     .E53_SC1_IICReadFunc = sc1E53_IICRead,
   //     .E53_SC1_IICWriteReadFunc = sc1E53_IICWriteRead,
   // };
   
   static int32_t Hdf_E53_SC1_DriverInit(struct HdfDeviceObject *device)
   {
       return HDF_SUCCESS;
   }
   
   // 驱动资源释放的接口
   void Hdf_E53_SC1_DriverRelease(struct HdfDeviceObject *deviceObject)
   {
       if (deviceObject == NULL) {
           HDF_LOGE("Led driver release failed!");
           return;
       }
       HDF_LOGD("Led driver release success");
       return;
   }
   
   
   // 定义驱动入口的对象，必须为HdfDriverEntry（在hdf_device_desc.h中定义）类型的全局变量
   static struct HdfDriverEntry g_E53DriverEntry = {
       .moduleVersion = 1,
       .moduleName = "HDF_E53_SC1",
       .Bind = Hdf_E53_SC1_DriverBind,
       .Init = Hdf_E53_SC1_DriverInit,
       .Release = Hdf_E53_SC1_DriverRelease,
   };
   
   // 调用HDF_INIT将驱动入口注册到HDF框架中
   HDF_INIT(g_E53DriverEntry);
   ```

3. 驱动配置驱动设备描述HDF框架加载驱动所需要的信息来源于HDF框架定义的驱动设备描述，因此基于HDF框架开发的驱动必须要在HDF框架定义的device_info.hcs配置文件中添加对应的设备描述，所以我们需要在device\st\bearpi_hm_micro\liteos_a\hdf_config\device_info\device_info.hcs中添加E53_SC1设备描述。 "##start##"和"##end##"之间为新增配置（"##start##"和"##end##"仅用来标识位置，添加完配置后删除这两行）

   ```C++
   device_adc :: device {
       device0 :: deviceNode {
           policy = 0;
           priority = 50;
           permission = 0644;
           moduleName = "HDF_PLATFORM_ADC_MANAGER";
           serviceName = "HDF_PLATFORM_ADC_MANAGER";
       }
       device1 :: deviceNode {
           policy = 0;
           priority = 55;
           permission = 0644;
           moduleName = "stm32mp157_adc_driver";
           deviceMatchAttr = "st_stm32mp157_adc";
       }
   }
   ##start##
   device_e53 :: device {  
   
           priority = 30;                
           device_sc1 :: deviceNode {             
               policy = 2;                     
               priority = 130; 
               preload = 1;                               
               permission = 0777;              
               moduleName = "HDF_E53_SC1";        
               serviceName = "hdf_e53_sc1";    
           }
   }
   ##end##
   ```

## JS接口层的适配

1. 添加控制E53_SC1的JS API接口修改

   ```
   foundation\ace\ace_engine_lite\frameworks\src\core\modules\app_module.h
   ```

   ，加入E53SC1Service JS API,（"##start##"和"##end##"仅用来标识位置，添加完配置后删除这两行）

   ```
   public:
   ACE_DISALLOW_COPY_AND_MOVE(AppModule);
   AppModule() = default;
   ~AppModule() = default;
   static JSIValue GetInfo(const JSIValue thisVal, const JSIValue *args, uint8_t argsNum);
   static JSIValue Terminate(const JSIValue thisVal, const JSIValue *args, uint8_t argsNum);
   ##start##
   static JSIValue E53SC1Service(const JSIValue thisVal, const JSIValue* args, uint8_t argsNum);
   ##end##
   #ifdef FEATURE_SCREEN_ON_VISIBLE
       static JSIValue ScreenOnVisible(const JSIValue thisVal, const JSIValue *args, uint8_t argsNum);
   #endif
   ```

   ```
   void InitAppModule(JSIValue exports)
   {
       JSI::SetModuleAPI(exports, "getInfo", AppModule::GetInfo);
       JSI::SetModuleAPI(exports, "terminate", AppModule::Terminate);
   ##start##
       JSI::SetModuleAPI(exports, "e53sc1service", AppModule::E53SC1Service);
   ##end##
   #ifdef FEATURE_SCREEN_ON_VISIBLE
       JSI::SetModuleAPI(exports, "screenOnVisible", AppModule::ScreenOnVisible);
   #endif
   }
   ```

2. 编写控制E53_SC1的c++ 业务代码在

   ```
   foundation\ace\ace_engine_lite\frameworks\src\core\modules\app_module.cpp
   ```

   中加入控制E53_SC1的c++ 业务代码（"##start##"和"##end##"仅用来标识位置，添加完配置后删除这两行）。

   ```
   #include "app_module.h"
   #include "ace_log.h"
   #include "js_app_context.h"
   #ifdef FEATURE_SCREEN_ON_VISIBLE
   #include "js_async_work.h"
   #include "product_adapter.h"
   #endif
   
   ##start##
   #include "hdf_sbuf.h"
   #include "hdf_io_service_if.h"
   
   #define E53_SC1_SERVICE "hdf_e53_sc1"
   ##end##
   
   namespace OHOS {
   namespace ACELite {
   const char * const AppModule::FILE_MANIFEST = "manifest.json";
   const char * const AppModule::KEY_APP_NAME = "appName";
   const char * const AppModule::KEY_VERSION_NAME = "versionName";
   const char * const AppModule::KEY_VERSION_CODE = "versionCode";
   ```

   ```
   struct AsyncParams : public MemoryHeap {
       ACE_DISALLOW_COPY_AND_MOVE(AsyncParams);
       AsyncParams() : result(nullptr), callback(nullptr), context(nullptr) {}
   
       JSIValue result;
       JSIValue callback;
       JSIValue context;
   };
   #endif
   
   ##start##
   
   static int E53SC1Control(struct HdfIoService *serv, int32_t cmd, const char* buf, char **val)
   {
       int ret = HDF_FAILURE;
       struct HdfSBuf *data = HdfSBufObtainDefaultSize();
       struct HdfSBuf *reply = HdfSBufObtainDefaultSize();
   
       if (data == NULL || reply == NULL) {
           HILOG_ERROR(HILOG_MODULE_ACE,"fail to obtain sbuf data\n");
           return ret;
       }
   
       if (!HdfSbufWriteString(data, buf))
       {
           HILOG_ERROR(HILOG_MODULE_ACE,"fail to write sbuf\n");
           HdfSBufRecycle(data);
           HdfSBufRecycle(reply);
           return ret;
       }
   
       ret = serv->dispatcher->Dispatch(&serv->object, cmd, data, reply);
       if (ret != HDF_SUCCESS)
       {
           HILOG_ERROR(HILOG_MODULE_ACE,"fail to send service call\n");
           HdfSBufRecycle(data);
           HdfSBufRecycle(reply);
           return ret;
       }
   
       *val = (char *)(HdfSbufReadString(reply));
       if(val ==NULL){
           HILOG_ERROR(HILOG_MODULE_ACE,"fail to get service call reply\n");
           ret = HDF_ERR_INVALID_OBJECT;
           HdfSBufRecycle(data);
           HdfSBufRecycle(reply);
           return ret;
   
       }
   
       HILOG_ERROR(HILOG_MODULE_ACE,"Get reply is: %s\n", *val);
   
       HdfSBufRecycle(data);
       HdfSBufRecycle(reply);
       return ret;
   }
   
   JSIValue AppModule::E53SC1Service(const JSIValue thisVal, const JSIValue *args, uint8_t argsNum)
   {
       struct HdfIoService *serv = HdfIoServiceBind(E53_SC1_SERVICE);
       if (serv == NULL)
       {
           HILOG_ERROR(HILOG_MODULE_ACE,"fail to get service %s\n", E53_SC1_SERVICE);
           return JSI::CreateUndefined();
       }
   
       if ((args == nullptr) || (argsNum == 0) || (JSI::ValueIsUndefined(args[0]))) {
           return JSI::CreateUndefined();
       }
   
       JSIValue success = JSI::GetNamedProperty(args[0], CB_SUCCESS);
       JSIValue fail = JSI::GetNamedProperty(args[0], CB_FAIL);
       JSIValue complete = JSI::GetNamedProperty(args[0], CB_COMPLETE);
   
       int32_t cmd = (int32_t)JSI::GetNumberProperty(args[0], "cmd");  
       char *data = (char *)JSI::GetStringProperty(args[0], "data");
       HILOG_ERROR(HILOG_MODULE_ACE, "cmd is: %d\n", cmd);
       HILOG_ERROR(HILOG_MODULE_ACE,"data is: %s\n", data);
       char *replyData;
   
       if (E53SC1Control(serv, cmd, data, &replyData))
       {
           HILOG_ERROR(HILOG_MODULE_ACE,"fail to send event\n");
           JSI::CallFunction(fail, thisVal, nullptr, 0);
           JSI::CallFunction(complete, thisVal, nullptr, 0);
           JSI::ReleaseValueList(success, fail, complete);
           return JSI::CreateUndefined();
       }
   
       JSIValue result = JSI::CreateObject();
   
       JSI::SetStringProperty(result, "e53_sc1", replyData);
       JSIValue argv[ARGC_ONE] = {result};
       JSI::CallFunction(success, thisVal, argv, ARGC_ONE);
       JSI::CallFunction(complete, thisVal, nullptr, 0);
       JSI::ReleaseValueList(success, fail, complete, result);
   
       HdfIoServiceRecycle(serv);
   
       return JSI::CreateUndefined();
   }
   
   ##end##
   
   JSIValue AppModule::GetInfo(const JSIValue thisVal, const JSIValue *args, uint8_t argsNum)
   {
       JSIValue result = JSI::CreateUndefined();
   
       cJSON *manifest = ReadManifest();
       if (manifest == nullptr) {
           HILOG_ERROR(HILOG_MODULE_ACE, "Fail to get the content of manifest.");
           return result;
       }
   
       cJSON *appName = cJSON_GetObjectItem(manifest, KEY_APP_NAME);
       cJSON *versionName = cJSON_GetObjectItem(manifest, KEY_VERSION_NAME);
       cJSON *versionCode = cJSON_GetObjectItem(manifest, KEY_VERSION_CODE);
   
       result = JSI::CreateObject();
       if (appName != nullptr && appName->type == cJSON_String) {
           JSI::SetStringProperty(result, KEY_APP_NAME, appName->valuestring);
       }
       if (versionName != nullptr && versionName->type == cJSON_String) {
           JSI::SetStringProperty(result, KEY_VERSION_NAME, versionName->valuestring);
       }
       if (versionCode != nullptr && versionCode->type == cJSON_Number) {
           JSI::SetNumberProperty(result, KEY_VERSION_CODE, versionCode->valuedouble);
       }
       cJSON_Delete(manifest);
       manifest = nullptr;
       return result;
   }
   ```

3. 配置HDF头文件路径在

   ```
   foundation\ace\ace_engine_lite\ace_lite.gni
   ```

   中添加HDF头文件路径。

   ```
   ace_lite_include_dirs += [
       "//drivers/framework/ability/sbuf/include",
       "//drivers/framework/include/core",
       "//drivers/framework/include/utils",
       "//drivers/adapter/uhdf/posix/include",
   ]
   ```

4. 添加编译依赖修改

   ```
   foundation\ace\ace_engine_lite\frameworks\BUILD.gn
   ```

   ，在

   ```
   public_deps
   ```

   中添加以下代码

   ```
   "//drivers/adapter/uhdf/manager:hdf_core",
   ```

   修改

   ```
   foundation\ace\ace_engine_lite\test\ace_test_config.gni
   ```

   ，在

   ```
   extra_deps
   ```

   中添加以下代码

   ```
   
   "//drivers/adapter/uhdf/manager:hdf_core",
   ```

   ## 编译烧录

1. 在MobaXterm中输入以下指令，进入源码根目录

   

   ```text
   cd /home/bearpi/project/bearpi-hm_micro_small/
   ```

2. 在MobaXterm中输入:

   

   ```text
   hb set 
   ```

   再输入"."(点)

   

   ```text
   .
   ```

   选择“bearpi-hm_micro”，然后回车

   

3. 在MobaXterm中输入：

   

   ```text
   hb build -t notest --tee -f
   ```

   然后回车，等待直到屏幕出现：`build success`字样，说明编译成功。

4. 查看编译出的固件位置

   当编译完后，在Windows中可以直接查看到最终编译的固件，具体路径在： `\project\bearpi-hm_micro_small\out\bearpi_hm_micro\bearpi_hm_micro` 其中有以下文件是后面烧录系统需要使用的。

   - OHOS_Image.stm32：系统镜像文件
   - rootfs_vfat.img：根文件系统
   - userfs_vfat.img：用户文件系统

   

   *注意，最前的磁盘在为`RaiDrive`映射的路径。

5. 在MobaXterm中执行以下三条指令将以上三个文件复制到`applications/BearPi/BearPi-HM_Micro/tools/download_img/kernel/`下，以便后续烧录系统使用

   

   ```text
   cp out/bearpi_hm_micro/bearpi_hm_micro/OHOS_Image.stm32 applications/BearPi/BearPi-HM_Micro/tools/download_img/kernel/
   cp out/bearpi_hm_micro/bearpi_hm_micro/rootfs_vfat.img applications/BearPi/BearPi-HM_Micro/tools/download_img/kernel/
   cp out/bearpi_hm_micro/bearpi_hm_micro/userfs_vfat.img applications/BearPi/BearPi-HM_Micro/tools/download_img/kernel/
   ```

   

## [八、连接开发板](https://www.bearpi.cn/dev_board/bearpi/hm/micro/software_OpenHarmony/BearPi-HM_Micro十分钟上手.html#八、连接开发板)

1. 通过TypeC数据线，把电脑与BearPi-HM Micro连接。

2. 安装CH340驱动。

   下载地址： [点击跳转](https://www.bearpi.cn/dev_board/bearpi/hm/micro/filebrowser/?path=6440728&filename=CH341SER.EXE)

3. 关闭虚拟机捕获USB功能。（有很多开发者都是因为虚拟机捕获了USB设备，导致本机Windows电脑看不到串口）

   

   如果上面操作不行，直接关闭VMware Workstation，选择挂起，然后再重新插拔USB。

4. 查看开发板的串口

   

## [九、烧录镜像](https://www.bearpi.cn/dev_board/bearpi/hm/micro/software_OpenHarmony/BearPi-HM_Micro十分钟上手.html#九、烧录镜像)

1. 将开发板的拨码开关上拨到“000”烧录模式，并按一下开发板的RESET按键

   

2. 在Windows打开STM32CubeProgrammer工具，选择“USB”烧录方式，再点击刷新按钮，然后点击"Connect"。

   

3. 点击STM32CubeProgrammer工具的“+”按钮，然后选择烧录配置的tvs文件。

   

4. 点击Browse按钮，然后选择工程源码下的烧录镜像路径

   

5. 点击Download按钮启动镜像烧录，并等待烧录完毕。

   

## [十、启动开发板进入终端](https://www.bearpi.cn/dev_board/bearpi/hm/micro/software_OpenHarmony/BearPi-HM_Micro十分钟上手.html#十、启动开发板进入终端)

1. 打开MobaXterm，

   1. 点击：`Session`、`Serial`按钮
   2. 设置Seral port为 第八-4 查看到的COM号
   3. 设置Speed为 `115200`
   4. 点击`OK`

   

2. 将开发板的拨码开关拨至“010”emmc启动模式，并按一下开发板的“RESET”按钮复位开发板。

   

3. MobaXterm上打印出运行日志，等待启动日志运行结束，按键盘“回车”按键进入开发板shell终端，输入例如"ls"命令，可与开发板交互。

   

4. 开发板屏幕出现桌面及预安装的"setting"应用，点击"setting"应用可查看系统信息。

   ##  安装Hap应用

以安装一个控制灯开关的应用为例讲解如何在开发板上安装HAP应用.

## [一、准备工作](https://www.bearpi.cn/dev_board/bearpi/hm/micro/software_OpenHarmony/如何在开发板上安装HAP应用.html#一、准备工作)

- 准备一张TF卡（需要格式化为FAT32），以及一个读卡器
- 如果没有TF卡，以下也有另一种复制文件的方法。

## [二、挂载TF卡](https://www.bearpi.cn/dev_board/bearpi/hm/micro/software_OpenHarmony/如何在开发板上安装HAP应用.html#二、挂载tf卡)

1. 将applications/BearPi/BearPi-HM_Micro/tools/hap_tools/hap_example路径下的bm、LED_1.0.0.hap拷贝到SD卡中

2. 将SD卡插入到开发板中，并按开发板的RESET按键重启开发板

3. 输入以下命令，挂载SD卡

   

   ```text
   mount /dev/mmcblk0p0 /sdcard vfat
   ```

4. 输入以下命令，进入SD卡目录

   

   ```text
   cd /sdcard
   ```

## [三、无TF卡复制文件（可选）](https://www.bearpi.cn/dev_board/bearpi/hm/micro/software_OpenHarmony/如何在开发板上安装HAP应用.html#三、无tf卡复制文件-可选)

1. 按下开发板的RESET按钮重启开发板

2. 在命令行看到提示“Hit any key to stop autoboot”时，按下回车按钮。可以看到输入命令提示。此时开发板停留在uboot阶段，尚未进入系统。

   

3. 输入命令

   

   ```text
   ums 0 mmc 1
   ```

4. 此时电脑上会挂载很多个u盘。逐个尝试浏览每一个u盘，如果遇到提示格式化的就略过，不要格式化。直到看到一个u盘内的文件夹目录如图所示。（2022_08_23.txt为测试用文件）

   

5. 将applications/BearPi/BearPi-HM_Micro/tools/hap_tools/hap_example路径下的bm、LED_1.0.0.hap拷贝到根目录。

6. 重启开发板，无视uboot提示，正常进入系统。

## [四、安装HAP应用](https://www.bearpi.cn/dev_board/bearpi/hm/micro/software_OpenHarmony/如何在开发板上安装HAP应用.html#四、安装hap应用)

1. cd进入之前拷贝文件的路径。输入以下命令，打开调试模式

   

   ```text
   ./bm set -s disable
   ./bm set -d enable
   ```

2. 安装应用

   

   ```text
   ./bm install -p LED_1.0.0.hap
   ```

注: LED_1.0.0.hap为安装包名称，安装其他应用需要修改为对应的安装包名称。

点击屏幕图标可以对E53_SC1进行控制。